BIG_approach_parliament = {

	BIG_raise_economic_law = {
		icon = generic_industry
		available = { always = yes }
		visible = { always = yes }
		fire_only_once = no
		days_remove = 10

		cost = 20
		remove_effect = {
			parliament_check_econ = yes

			if = {
				limit = { 
					check_variable = { 
						var = yes
						value = 10
						compare = greater_than_or_equals
					}
				}
				country_event = { id = bigparliament.1 }
			}
			
			if = {
				limit = { 
					check_variable = { 
						var = yes
						value = 10
						compare = less_than
					}
				}
				country_event = { id = bigparliament.2 }
			}
			
			clear_variable = yes
		}

		ai_will_do = {
			base = 0
		}
	}
	
	BIG_lower_economic_law = {
		icon = generic_industry
		available = { always = yes }
		visible = { always = yes }
		fire_only_once = no
		days_remove = 60

		cost = 20
		remove_effect = {
			lower_economy_law = yes
		}

		ai_will_do = {
			base = 0
		}
	}
	
	BIG_raise_manpower_law = {
		icon = generic_industry
		available = { always = yes }
		visible = { always = yes }
		fire_only_once = no
		days_remove = 60

		cost = 80
		remove_effect = {
		}

		ai_will_do = {
			base = 0
		}
	}
	
	BIG_lower_manpower_law = {
		icon = generic_industry
		available = { always = yes }
		visible = { always = yes }
		fire_only_once = no
		days_remove = 60

		cost = 80
		remove_effect = {
		}

		ai_will_do = {
			base = 0
		}
	}
}