add_namespace = test

country_event = {
	id = test.1
	title = test.t
	desc = test.d
	picture = GFX_news_event_003
	is_triggered_only = yes
	
	option = {
		name = test.a
		every_country = {
			if = {
				limit = { 
					tag = YAK
					tag = EQA
					tag = CRY
				}
				set_country_flag = cry_region
			}
		}
	}
}