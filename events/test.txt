add_namespace = rp

country_event = {
	id = rp.1
	hidden = yes
	is_triggered_only = yes

	immediate = {
		random_country = {
			if = {
				limit = { tag = YAK tag = CRY tag = EQA tag = GPA }
				set_country_flag = crystal_mountains_region
			}
		}
	}
}